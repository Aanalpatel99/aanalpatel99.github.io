title: Aanal Patel
description: Embedded AI Engineer & Applied AI Researcher building edge AI, computer vision, and RAG systems
url: "https://aanalpatel99.github.io"
lang: en

twitter:
  card: summary_large_image

# github-pages defaults to jekyll-theme-primer unless explicitly disabled —
# this site uses its own hand-built layout/CSS, not a Pages theme.
theme: null

author:
  name: Aanal Patel
  email: aanal.patel1199@gmail.com

github_username: aanalpatel99
linkedin_url: "https://www.linkedin.com/in/aanalpatel21/"

# GoatCounter analytics — sign up free at https://www.goatcounter.com,
# pick a site code (yields https://<code>.goatcounter.com), then uncomment
# and set it below. Left unset, no analytics script is emitted at all.
# goatcounter_code: "your-code-here"

plugins:
  - jekyll-seo-tag
  - jekyll-sitemap

# Markdown/posts
markdown: kramdown
permalink: /blog/:title/

collections:
  posts:
    output: true

defaults:
  - scope:
      path: ""
    values:
      image: /assets/img/og-image.png
  - scope:
      path: ""
      type: posts
    values:
      layout: post
